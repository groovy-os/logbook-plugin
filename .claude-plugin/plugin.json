{
  "name": "logbook",
  "version": "0.3.0",
  "description": "Run the agentic engineering loop against a logbook board: file build-ready logs, explore blast radius, spec UX, plan collision-aware batches, and drive build \u2192 review \u2192 fix \u2192 merge with an orchestrator and subagents.",
  "author": {
    "name": "groovy-os",
    "url": "https://github.com/groovy-os"
  },
  "homepage": "https://www.logbk.dev",
  "repository": "https://github.com/groovy-os/logbook-plugin",
  "license": "LicenseRef-Proprietary",
  "keywords": [
    "logbook",
    "backlog",
    "mcp",
    "orchestration",
    "agentic-engineering"
  ],
  "userConfig": {
    "baseBranch": {
      "type": "string",
      "title": "Base branch",
      "description": "Branch that feature branches cut from and pull requests target.",
      "default": "main"
    },
    "testCommand": {
      "type": "string",
      "title": "Test command",
      "description": "Command that runs the test suite for a scoped change. Leave blank to have the skills detect it from the repo.",
      "default": ""
    },
    "webhookWired": {
      "type": "boolean",
      "title": "GitHub merge webhook is configured",
      "description": "Leave false unless you have wired the logbook webhook to a GitHub repo. False means the loop closes logs itself when work finishes. True means it waits for a merged pull request to close them \u2014 which never happens without the webhook, so finished work would sit open forever.",
      "default": false
    }
  }
}
